<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color -->
    <solid android:color="#FFFFFF" />

    <!-- Rounded corners -->
    <corners android:radius="16dp" />

    <!-- Optional soft shadow-like border -->
    <stroke
        android:width="1dp"
        android:color="#E0E0E0" />
</shape>
